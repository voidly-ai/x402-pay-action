{
  "name": "x402-pay-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action — pay any HTTP 402 Voidly Pay endpoint from CI.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.mjs -o dist --license licenses.txt",
    "format": "prettier --write src/"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/voidly-ai/x402-pay-action"
  },
  "dependencies": {
    "@actions/core": "^1.11.0",
    "@voidly/pay": "^0.1.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
